# expected: workflow-level id-token: write
name: invalid release permissions
permissions: write-all
jobs:
  validate:
    runs-on: ubuntu-latest
    steps: []
  attest:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps: []
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps: []
